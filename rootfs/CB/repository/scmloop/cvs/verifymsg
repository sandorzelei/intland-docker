# Please, add/uncomment the appropriate line below to CVSROOT/verifymsg!
#
# Windows (Replace path "C:\scmloop" with appropriate path):
#DEFAULT cmd /c C:\scmloop\scmloop.bat -mode verifymsg
#
# Unix (Replace path "/usr/local/bin" with appropriate path):
#DEFAULT /usr/local/bin/scmloop -repositoryId ${repositoryId} -mode verifymsg
#
# cvs-1.12.x or higher:
#DEFAULT /usr/local/bin/scmloop -repositoryId ${repositoryId} -mode verifymsg %l
